/*
import AdminBro from 'admin-bro'

// !! hacer esto despues usando assertion, o sea, es para saber si value implementa una interfaz
// (value as ConvectorModel).someproperty !== 'undefined'
class HypoResource extends AdminBro.BaseResource {

    static isAdapterFor(value: any) : boolean {
        return true
    }

}
*/